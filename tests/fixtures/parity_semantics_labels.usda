#usda 1.0
(
    defaultPrim = "World"
)

def Xform "World"
{
    def Xform "Kitchen"
    {
        def Xform "CupBlue"
        {
            token[] semantics:labels:somaHome = ["Crockery", "DesignedContainer"]
            token[] semantics:labels:engine = ["pickup", "container"]
        }

        def Xform "Stove"
        {
            token[] semantics:labels:somaHome = ["Appliance"]
        }
    }
}
