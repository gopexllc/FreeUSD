# OpenUSD module map (FreeUSD)

FreeUSD mirrors the **library boundaries** and common **public concepts** of OpenUSD (pxr) for interoperability and learning. Implementations are **clean-room** and intentionally smaller until features land.

For **repository layout**, CMake target naming, and which upstream trees are out of scope, see [openusd-repo-alignment.md](openusd-repo-alignment.md).

| OpenUSD (pxr) area | FreeUSD | Notes |
| --- | --- | --- |
| `tf` (tokens, hashing) | `freeusd::tf` | `Token` interning; `HashString` for maps |
| `gf` (linear algebra) | `freeusd::gf` | `Vec3d`, **`Vec3f`** (`float3`), `Matrix4d` (**`Translate` / `Shear` / `Scale` / `Multiply`**, **`GetInverse`**, **`RotateDegrees*`** axis + packed Euler, **`FromUnitQuaternion`** for ``quatd`` / ``quatf`` / ``xformOp:orient``), **`Quatd`** / **`Quatf`** (**`Normalized`**), **`BBox3d`**, **`Range1d`**; Python **`freeusd.gf`** (`Vec3d`, **`Vec3f`**, `Matrix4d` + **`Shear`**, **`get_inverse`**, **`BBox3d`**, **`Quatd`**, **`Quatf`**, **`Range1d`**) |
| `vt` (typed values) | `freeusd::vt` | `Value` variant for layer payloads (includes **`token[]`**, **`quatd`**, **`quatf`**, **`vec3f`**, **`matrix4d`** / **`MakeMatrix4d`** / **`GetMatrix4d`** / **`HoldsMatrix4d`**); C++ **`Holds*`** / **`Get*`**; Python **`make_quatd`**, **`make_quatf`**, **`make_matrix4d`**, **`holds_quatd`**, **`holds_quatf`**, **`holds_matrix4d`**, **`as_quatd`**, **`as_quatf`**, **`as_matrix4d`**, **`make_token_array`**, …, **`as_vec3d`**, **`make_vec3f`**, **`holds_vec3f`**, **`as_vec3f`** |
| `ar` (asset resolution) | `freeusd::ar` | `Resolver` + `DefaultResolver` (filesystem); **`freeusd/ar/resolvedPath.hpp`** + Python **`ar.ResolvedPath`**; **`DefaultResolver.resolve_path`** → **`ResolvedPath`** (alongside string **`resolve`**) |
| `sdf` (scene description) | `freeusd::sdf` | `Path`, `Layer`, `FieldOpinion` (**`EvaluateAt`**: linear **float/double/int32/int64**, **vec3d** / **vec3f** / **matrix4d** (per-element), **quatd** / **quatf** (**slerp**); non-interpolable types use lower bracket; before first sample → default else earliest), prim **`customData`**, **attribute connections** (`*.connect`), prim **references**, **`inherits` / `specializes` / `payload`** arc lists (authored order; USDA `prepend`/`append`/`=` list ops in prim metadata), prim **relationships** (`Set`/`Prepend`/`Append`/`DeleteRelationshipTargets`); prim **`variantSelection`** / **`variantSets`** (USDA metadata dictionaries); **`freeusd/sdf/layerOffset.hpp`** + **`sdf.LayerOffset`**; **`freeusd/sdf/assetPath.hpp`** + **`sdf.AssetPath`**; header **`freeusd/sdf/tokens.hpp`** + Python **`freeusd.sdf.builtin_tokens`** / **`_native.sdf.builtin_tokens`** (common field-name tokens) |
| `pcp` (composition) | `freeusd::pcp` | `LayerStack`, **`ComposeSublayers`** / **`ComposeSublayersDepthFirst`**, **`ResolveFieldAtTimeStrongestWins`** (layer-stack falloff) |
| `usd` (stage graph) | `freeusd::usd` | **`Stage::AttachLayerStack`**: ordered layers (strong→weak), attributes with **strongest opinion** (defaults + **time samples**); **composed time-sample union** per field; **attribute `.connect`**: follows **first authored connection** from strong→weak per hop, then reads the target property (cycle-limited); **relationship targets concatenate** when each layer contributes a list; **composed field-name / relationship-name unions** across layers; **composed prim-path union**; composed **`references` / `inherits` / `specializes` / `payloads`** lists concatenate per layer (stronger first), with composed **relocates** now rewriting prim-path queries and composed **`prefixSubstitutions`** rewriting returned reference/payload asset paths; composed prim **`variantSelection`** (strongest wins) + **`variantSets`** name lists / per-set variant name lists on **`Stage`**; **`Stage::OpenFromRootFile`** loads USDA from disk and optionally stacks **`subLayers`** (shallow or depth-first via **`RootLayerSublayersPolicy`**, with Python **`Stage.open_from_root_file`**); Python **`Stage.read_field_*`** helpers for composed scalars, **`double3`/`float3`**, **`matrix4d`**, **`quatd`**, **`quatf`**, **`token`**, and **`token[]`** at a time (see [openusd-repo-alignment.md](openusd-repo-alignment.md)); root-layer **defaultPrim** name; **prim kind / active** / **`customData`** as above; header **`freeusd/usd/kindTokens.hpp`** + Python **`freeusd.usd.kind_tokens`** for usual **model `kind`** strings (`component`, `assembly`, …); **`freeusd/usd/tokens.hpp`** + **`freeusd.usd.builtin_tokens`** (composition list / variant / relocate field names); **`freeusd::usd::TimeCode`** (`timeCode.hpp`) + Python **`freeusd.usd.TimeCode`** (UsdTimeCode-shaped sentinels); **`freeusd::usd::EditTarget`** (`editTarget.hpp`) + Python **`freeusd.usd.EditTarget`** (UsdEditTarget-shaped layer handle); **`freeusd/usd/schemaDataTokens.hpp`** + **`freeusd.usd.schema_data_tokens`** from published **`pxr/usd/usd/generatedSchema.usda`** (core prim/API schema strings; **`usd/tokens.hpp`** + **`builtin_tokens`** stay the hand-authored Sdf-style composition field names) |
| `usdUtils` | `freeusd::usdUtils` | Header **`freeusd/usdUtils/pipeline.hpp`** (`FlattenOptions` + **`FlattenStageAtTime`**) plus **`freeusd/usdUtils/engineScene.hpp`** (`BuildEngineSceneSnapshot`, `BuildEnginePrimEditorView`, `AssessEngineRuntimeSupport`) |
| `usdGeom` | `freeusd::usdGeom` | **Generated** **`tokens.hpp`** …; **`Imageable`** computes inherited **`purpose`** / **`visibility`**; **`Xformable`** reads **`xformOpOrder`** as **`token[]`**, single **`token`**, comma-separated **`string`**, or empty-order fallback; plus translate / **`xformOp:translate:pivot`** / **`xformOp:translate:pivotInverse`** / scale / Euler / axis rotates / **`xformOp:orient`** (**`quatd`** / **`quatf`**) / **`xformOp:transform`** (**`matrix4d`**) / **`xformOp:shear`** (**`vec3d`**, upper-triangular **`gf::Matrix4d::Shear`**) and OpenUSD-style **`!invert!`** / **`!resetXformStack!`** markers (reset trims local op list through the last marker; L2W stops at resetting prims); **`ComputeParentToWorldTransform`**; **`Boundable`** computes local/world bounds for the current tiny executable subset; Python **`freeusd.usdGeom.Imageable`** / **`freeusd.usdGeom.Xformable`** / **`freeusd.usdGeom.Boundable`** |
| `usdShade` | `freeusd::usdShade` | **Generated** schema **tokens** (no shading network logic); Python **`freeusd.usdShade.tokens`** |
| `usdLux` | `freeusd::usdLux` | **Generated** schema **tokens**; no light schema logic |
| `usdPhysics` | `freeusd::usdPhysics` | **Generated** schema **tokens**; no simulation |
| `usdVol` | `freeusd::usdVol` | **Generated** schema **tokens**; no volume engine |
| `usdSkel` | `freeusd::usdSkel` | **Generated** **`tokens.hpp`**; **`Skeleton`** / **`SkelAnimation`** read joints, bind/rest matrices, sampled TRS; **`SkelBinding`** / **`SkelRoot`** resolve skinning and animation links; **`BlendShape`** / **`SkelBlendShapes`** / **`MorphTargets`** apply CPU morphs; **`DeformPointsWithSkeleton`** LBS; **`gltfMapping`** parent indices + world bind helpers |
| `usdMedia` | `freeusd::usdMedia` | **Generated** schema **tokens** |
| `usdRender` | `freeusd::usdRender` | **Generated** schema **tokens** |
| `usdRi` | `freeusd::usdRi` | **Generated** schema **tokens** |
| `usdHydra` | `freeusd::usdHydra` | **Generated** schema **tokens** (Hydra-facing schema names only; no Hd engine) |
| `usdMtlx` | `freeusd::usdMtlx` | **Generated** schema **tokens** |
| `usdProc` | `freeusd::usdProc` | **Generated** schema **tokens** |
| `usdSemantics` | `freeusd::usdSemantics` | **Generated** schema **tokens** |
| `usdUI` | `freeusd::usdUI` | **Generated** schema **tokens** (includes colon-namespaced field names as `TfToken` strings) |
| **Usd schema umbrella (opt-in link)** | `freeusd::usd_schemas` | INTERFACE aggregate of **`usdGeom`** + **`usdShade`**, **`usdLux`**, **`usdPhysics`**, **`usdVol`**, **`usdSkel`**, **`usdMedia`**, **`usdRender`**, **`usdRi`**, **`usdHydra`**, **`usdMtlx`**, **`usdProc`**, **`usdSemantics`**, **`usdUI`** (generated **`tokens.hpp`** each); optional C++ bundle **`freeusd/usd/schemaTokens.hpp`** (also includes **`freeusd/usd/schemaDataTokens.hpp`** from **`pxr/usd/usd/generatedSchema.usda`**) |
| `plug` | `freeusd::plug` | `Registry` no-op loader; Python **`freeusd.plug`** (`load_all`) |
| `trace` | `freeusd::trace` | No-op `Collector` + `FREEUSD_TRACE_FUNCTION`; Python **`freeusd.trace`** (`push` / `pop`) |
| `work` | `freeusd::work` | Serial `Dispatcher` stub; Python **`freeusd.work.run`** |
| **FFI (Go / Rust)** | `bindings/go`, `bindings/rust/freeusd-sys` | Thin wrappers over **`libfreeusd_c`** + static archives (see [`bindings/README.md`](../bindings/README.md)) |
| **pkg-config** | `freeusd.pc` (install) | After **`cmake --install`**, **`pkg-config --libs freeusd`** for Make / non-CMake consumers (static archives; GNU **`--start-group`** on Linux / MinGW only); see [engine-integration.md](engine-integration.md) |
| **CPack** | root `CMakeLists.txt` | **`cpack`** from the build directory produces **TGZ** (Unix) or **ZIP** (Windows) archives with headers, static libraries, CMake config, **`freeusd.pc`**, and **`LICENSE`**; see [engine-integration.md](engine-integration.md) |
| **C ABI** | `libfreeusd_c` + `include/freeusd/c/freeusd.h` | Stable C entry points: layer **identifier / documentation / defaultPrim / subLayers / prim paths** + **layer stack** + stage attach, **`freeusd_stage_open_from_root_file_utf8`** (USDA plus narrow embedded-`USDA` crate fallback), USDA I/O, field reads (**double**/**float**/**bool**/**int64**/string/**vec3d**/**vec3f**) + **composed sample-time** lists, **field-opinion** + **`.connect` target** query, **composed prim path** union + child paths + **concatenated relationship targets** + **composed layer `relocates`** and **`prefixSubstitutions`** (key-in-layer query, composed target prefix, sorted U+001F-separated pair list) + **composed references / inherits / specializes / payloads** (string lists + `has_*`) + **composed field / rel name lists**, composed **active / active-opinion** / **kind** / **customData** + **key list / key-in-layer**, composed stage **`customLayerData`** (string/token via C ABI + key list), composed prim **`variantSelection`** / **`variantSets`** (C ABI string lists + `has_*`-style queries), composed **`def` / `class` / `over`** specifier (`freeusd_stage_resolve_prim_specifier_kind` + `FreeusdPrimSpecifierKind`), root **defaultPrim**, **pseudo-root** path, composed **layer time / units / `upAxis` / `primOrder`** hints, `usdGeom`-subset transform / visibility / purpose / bounds helpers, plus raw **USDC section bytes** with **`freeusd_read_usdc_section_bytes_from_path_utf8`** / **`freeusd_bytes_free`** |
| Crate (binary) | `freeusd::usd::crate` | **`UsdcCrateIdentifier()`** (`PXR-USDC`), **`DetectUsdFileKindFromPath`** (USDA vs crate magic), **`ReadUsdCrateBootstrapFromPath`**, **`ReadUsdCrateTocFromPath`**, **`ReadUsdCrateSectionBytesFromPath`**, validated **`TOKENS`** / **`STRINGS`** / **`PATHS`** table decode, and a narrow **`USDA`**-section bridge for stage open; still no full spec-level binary scene decode |
| USDA (ASCII) | `freeusd::io::usda` | Minimal load/save (`def`, typed attrs, …); bracketed **`[@token, …]`** token arrays (**`@name@`** elements store **`name`** without the surrounding **`@`**); scalar **`float`** / **`half`** (authored numeric literals) store **`vt::Value`** as **`float`** (save uses the **`float`** type hint for all float payloads, including authored **`half`**); parenthesized **`(w, i, j, k)`** **quatd** / **quatf** (typed on the attribute line; **quatf** uses float components; **double3** is three comma-separated numbers); **`float3`** / **`point3f`** / **`normal3f`** / **`color3f`** literals (same parenthesized tuple) store **`vec3f`**; nested **`((…), (…), (…), (…))`** **matrix4d** (four rows of four doubles, row-major **`gf::Matrix4d`**); relationship list ops **`prepend` / `append` / `delete rel`** |
| **UsdImaging / Hydra / Hd*** (imaging stack) | n/a | **Not present**; reserved for future clean-room work |
| **Ndr / Sdr** (shader discovery) | n/a | **Not present** |

This is **not** API-compatible with Pixar’s OpenUSD at the ABI or exhaustive method level; it tracks **roles** and a **usable subset** so the codebase can grow toward spec compliance without importing upstream sources.
